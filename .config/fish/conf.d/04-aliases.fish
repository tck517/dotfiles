# Use abbreviations (expand when you hit space/enter) for git shortcuts
abbr -a gcb git checkout -b
abbr -a gcm git checkout main
abbr -a gpl git pull

# Regular aliases for command remapping
alias python python3
alias pip pip3
alias be "bundle exec"
alias cl clear
alias tf terraform
alias hb hub
alias claude "$HOME/.claude/local/claude"
